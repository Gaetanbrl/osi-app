import React from 'react'
import {  } from 'react-bootstrap';
// import ReactBootstrapSlider from 'react-bootstrap-slider'

const SlideBox = ({ setRef, territoire }) => {
	
	return(
	<div>
	</div>
	)
}

export default SlideBox
		// <ReactBootstrapSlider
		// value={2010}
		// change={null}
		// slideStop={10}
		// step={2}
		// max={2018}
		// min={2010}
		// orientation="horizontal"
		// reversed={false}
		// disabled="disabled" 
		// ticks = {[2010, 2012, 2014, 2016, 2018]}
		// ticks_labels = {["$2010", "$2012", "$2014", "$2016", "$2018"]}
		// ticks_snap_bounds = { 1 }/>